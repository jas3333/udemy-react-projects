import { FaBars } from 'react-icons/fa';
const Home = () => {
    return (
        <main>
            <FaBars className='icon' />
            <button className='btn'>SHOW MODAL</button>
        </main>
    );
};

export default Home;
